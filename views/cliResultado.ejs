<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Lista de clientes</title>
</head>
<body>
  <h1>Lista de clientes</h1>
  <ul>
    <% results.forEach((cliente)=> { %>
      <li>
        <strong>Nombre:</strong> <%= cliente.Nombre %><br>
        <strong>Apellido:</strong> <%= cliente.Apellido %><br>
        <strong>Cedula:</strong> <%= cliente.Cedula %>
      </li>
    <% }) %>
  </ul>

  <a href="/cli/registro" class="btn btn-primary">Registrar cliente</a> <!-- Botón para agregar clientes -->
  <a href="/menu" class="btn btn-primary">Volver al Menú</a>
</body>
</html>